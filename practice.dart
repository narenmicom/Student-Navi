import 'dart:math';

void main() {
  int present = 4;

  int total = 5;

  print("${(present / total * 100).round()}%");

  // String sub = "IOT";
  // var first = '50';

  // var subs = sub + first.toString();
  // print(subs);
  // var nextid = (int.parse(subs.substring(subs.length - 2)) + 1).toString();
  // if (nextid.length == 1) {
  //   nextid = '0$nextid';
  // }
  // sub = sub + nextid;
  // print(sub);
}
